---
description: Use this when you want to store long term memories
---

1. Ask the user for confirmation before saving the information to memory.
2. If confirmed, extract the key fact and invoke the remember script.
   // turbo
3. Run the following command: `npx ts-node scripts/remember.ts "<fact>"` in the `backend` directory.
